-- Using this, we can rework everything in G.P_CENTERS, so let's rework glass too
MP.ReworkCenter({
	key = "m_glass",
	ruleset = "sandbox",
	config = { Xmult = 2, extra = 3 }, -- Couldn't be simpler
})
